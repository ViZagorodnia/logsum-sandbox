[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
# E = pycodestyle errors, F = pyflakes (unused imports, undefined names…)
# Kata 5.5: focused rule set so CI output is readable and predictable.
select = ["E", "F"]
